---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<!DOCTYPE html>
<html lang="en">
<head>
  <BaseHead 
    title="404 - Page Not Found | Subo" 
    description="The page you're looking for doesn't exist. Return to Subo to create surveys and polls for your Discord community."
  />
  <style>
    body {
      background: linear-gradient(var(--dark), var(--secondary-dark));
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }
    
    main {
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 2rem;
      margin-top: 8rem;
    }
    
    .error-content {
      max-width: 600px;
      margin: 0 auto;
    }
    
    .error-number {
      font-size: 8rem;
      font-weight: 700;
      font-family: 'IBM Plex Mono', monospace;
      color: var(--primary);
      text-shadow: 0 0 20px rgba(255, 107, 107, 0.3);
      margin: 0;
      line-height: 1;
    }
    
    .error-title {
      font-size: 2.5rem;
      font-weight: 700;
      color: white;
      margin: 1rem 0;
    }
    
    .error-message {
      font-size: 1.2rem;
      color: var(--light);
      margin: 1.5rem 0 2.5rem 0;
      line-height: 1.6;
    }
    
    .error-actions {
      display: none;
      flex-direction: column;
      gap: 1rem;
      align-items: flex-start;
      margin-top: 1.5rem;
      max-width: 75%;
    }
    .error-actions.visible {
      display: flex;
      animation: fadeIn 0.3s;
    }
    .response-action-btn {
      background: var(--light);
      color: var(--dark);
      border: none;
      border-radius: 13px 13px 13px 3px;
      padding: 1.2rem 1.5rem;
      font-size: 1.1rem;
      font-weight: 600;
      text-align: left;
      cursor: pointer;
      transition: background 0.2s, box-shadow 0.2s;
      box-shadow: 0 2px 8px rgba(0,0,0,0.08);
      margin-bottom: 0.5rem;
      width: 100%;
      max-width: 100%;
    }
    .response-action-btn:hover {
      background: var(--primary);
      color: #fff;
      box-shadow: 0 4px 12px rgba(255, 107, 107, 0.18);
    }
    
    .button {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 1rem 2rem;
      border-radius: 0.5rem;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.2s;
      border: none;
      cursor: pointer;
    }
    
    .button--primary {
      background: var(--primary);
      color: white;
    }
    
    .button--primary:hover {
      background: var(--accent);
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(255, 107, 107, 0.3);
    }
    
    .button--secondary {
      background: rgba(255, 255, 255, 0.1);
      color: white;
      border: 1px solid rgba(255, 255, 255, 0.2);
    }
    
    .button--secondary:hover {
      background: rgba(255, 255, 255, 0.2);
      transform: translateY(-2px);
    }
    
    .subo-icon {
      width: 80px;
      height: 80px;
      margin-bottom: 2rem;
      filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.3));
    }
    
    .survey-options {
      display: flex;
      flex-direction: column;
      gap: 0.8rem;
      margin: 2rem 0;
      max-width: 400px;
      margin-left: auto;
      margin-right: auto;
    }
    
    .survey-option {
      background: rgba(255, 255, 255, 0.08);
      border: none;
      border-radius: 13px 13px 3px 13px;
      color: white;
      padding: 1rem 1.5rem;
      font-size: 1rem;
      cursor: pointer;
      transition: all 0.2s ease;
      text-align: left;
      font-family: inherit;
    }
    
    .survey-option:hover {
      background: rgba(255, 255, 255, 0.15);
      border-color: var(--primary);
      transform: translateY(-1px);
    }
    
    .survey-option.selected {
      background: var(--primary);
      border-color: var(--primary);
      color: white;
      font-weight: 600;
    }
    
    .response-message {
      background: var(--light);
      border: none;
      border-radius: 13px 13px 13px 3px;
      padding: 1.5rem;
      margin: 2rem 0;
      max-width: 75%;
      color: var(--dark);
      font-size: 1.1rem;
      line-height: 1.6;
      text-align: left;
    }
    
    @media (max-width: 600px) {
      .error-number {
        font-size: 6rem;
      }
      
      .error-title {
        font-size: 2rem;
      }
      
      .error-message {
        font-size: 1.1rem;
        margin-bottom: 50px;
      }
      
      .error-actions {
        padding-top: 50px;
        flex-direction: column;
        align-items: center;
      }
      
      .button {
        width: 100%;
        max-width: 280px;
        justify-content: center;
      }
    }
  </style>
</head>
<body>
  <Header />
  <main>
    <div class="error-content">
      <img src="/images/subo-neon2-100.png" alt="Subo" class="subo-icon" />
      <h1 class="error-number">404</h1>
      <h2 class="error-title">Whoa. This page doesn’t exist.</h2>
      <p class="response-message">Not sure how you got here, but now I’m curious. 👀</p>
      <p class="response-message">What's your story?</p>
      <div class="survey-options">
        <button class="survey-option" data-value="typo">I made a typo</button>
        <button class="survey-option" data-value="random">I like typing random URLs</button>
        <button class="survey-option" data-value="broken">A broken link</button>
        <button class="survey-option" data-value="confused">I really don't know what I'm doing</button>
        <button class="survey-option" data-value="other">Other</button>
      </div>
        <p class="response-message" id="responseMessage" style="display: none;"></p>
      <div class="error-actions" id="errorActions">
        <a href="/" class="response-action-btn">
          🏠 Go Home
        </a>
        <a href="/invite" class="response-action-btn" target="_blank" rel="noopener noreferrer">
          🤖 Add Subo to Discord
        </a>
      </div>
    </div>
  </main>
  <Footer />

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const options = document.querySelectorAll('.survey-option');
      const responseMessage = document.getElementById('responseMessage');
      const errorActions = document.getElementById('errorActions');

      const responses = {
        typo: "Ok, here is what you can do. Happens to the best of us! 😊",
        random: "Ok, here is what you can do. I respect the curiosity! 🎲",
        broken: "Ok, here is what you can do. Oh, and you encountered a broken link, do me a solid and report it in our support server. You'll make my day. 💬",
        confused: "Ok, here is what you can do. No worries, we've all been there! 🤗",
        other: "Ok, here is what you can do. Mystery solved... or not! 🕵️"
      };

      options.forEach(option => {
        option.addEventListener('click', function() {
          // Remove selected class from all options
          options.forEach(opt => opt.classList.remove('selected'));
          // Add selected class to clicked option
          this.classList.add('selected');
          // Show response message
          const value = this.getAttribute('data-value');
          responseMessage.textContent = responses[value];
          responseMessage.style.display = 'block';
          // Show error actions as styled buttons
          errorActions.classList.add('visible');
          // Smooth scroll to response
          responseMessage.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        });
      });
    });
  </script>
</body>
</html>